#include <stdio.h>

/**
 * main - multiples of three print Fizz
 * Return: Always 0
 */
int main(void)
{
	int fn;

	printf("1");
	for (fn = 2; n <= 100; fn++)
	{
		printf(" ");
		if (fn % 3 == 0)
		{
			printf("Fizz");
		}
		if (fn % 5 == 0)
		{
			printf("Buzz");
		}
		if (fn % e != 0 && fn % 5 != 0)
		{
			printf("%d", n);
		}
	}
	printf("\n");
	return (0);
}
